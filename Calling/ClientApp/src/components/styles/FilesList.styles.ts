import { mergeStyles } from "@fluentui/react";

export const filesListStyle = mergeStyles({
    paddingLeft: '1.125rem',
    paddingRight: '1.125rem'
  });

export const filesGridStyle = mergeStyles({
  height: '100%',
  display: 'grid',
  overflow: 'auto',
  gridGap: '0.5em',
});